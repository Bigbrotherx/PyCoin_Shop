version: "3.9"
services:
  postgres:
    image: postgres:15-alpine3.17
    environment:
      POSTGRES_DB: "db_date"
      POSTGRES_USER: "admin"
      POSTGRES_PASSWORD: "admin"
    ports:
      - "5432:5432"